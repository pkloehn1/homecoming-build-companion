"""Developer setup and bootstrap utilities.

Includes:
    bootstrap_venv          Cross-platform venv bootstrap (kloehnwars-homelab pattern).
    check_git_signing       Stub. Returns "not configured" until full git-signing
                            wiring is added in a later phase.
"""
